
message(STATUS "Finding Zlib:")
include("${CMAKE_CURRENT_LIST_DIR}/zlib-targets.cmake")
if(TARGET zlib AND NOT TARGET z)
    add_library(z ALIAS zlib)
endif()
#set(ZLIB_FOUND TRUE)
message(STATUS "Leaving finding ZLIB:")
